# Tests for ExprFloor and ExprAbsoluteValue.

test "floor":
	assert floor 2.9 is 2 with "floor 2.9 was not 2"
	assert floor -2.1 is -3 with "floor -2.1 was not -3"

test "absolute value":
	assert abs of -4 is 4 with "abs of -4 was not 4"
	assert absolute value of 4 is 4 with "abs of 4 was not 4"
